services:
  - type: web
    name: cictify
    env: docker
    region: singapore
    plan: starter
    autoDeploy: true
    healthCheckPath: /api/health
    dockerfilePath: Dockerfile
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
      - key: WEB_LIVE_FALLBACK
        value: "false"